// books.js
document.addEventListener('DOMContentLoaded', function () {
    console.log('Books JS cargado para el CPT Books');

    // Ejemplo: añadir interactividad
    const booksTitle = document.querySelector('.books-title');
    if (booksTitle) {
        booksTitle.style.color = 'blue'; // Cambiar el color del título
    }
});
